import React from 'react'

// const Name = (props) => {
//   return (
//     <div className='title'>{props.title}</div>
//   )
// }

const Title = (props) => {
  return (
    <div className='title'>{props.title}</div>
  )
}

export default Title
